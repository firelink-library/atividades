#import "../../../../themes/inteli/lib.typ": *

#problem()[
  Crie uma classe `Pessoa` em C++ com atributos privados `nome` (string) e `idade` (int). Implemente métodos públicos para definir e obter esses valores. No programa principal, crie dois objetos do tipo `Pessoa`, defina seus dados e imprima as informações de ambos.
]
