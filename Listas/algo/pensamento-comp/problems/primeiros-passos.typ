#import "../../../../themes/inteli/lib.typ": *

= Primeiros passos

#problem()[
  Escreva um programa em C++ que exiba a mensagem "Olá, mundo!" na tela.

  *Output esperado*
  ```
  Olá, mundo!
  ```
]

#problem()[
  Crie um programa em C++ que faça uma saudação personalizada para o usuário.

  Para isso, o usuário deve informar o seu nome e o programa deve exibir a
  saudação personalizada.

  *Exemplo de input*
  ```
  Digite o seu nome: André
  ```

  *Output esperado*
  ```
  Olá, André!
  ```
]

#problem()[
  Escreva um programa em C++ que imprima o padrão abaixo na tela usando múltiplos comandos de saída:

  *Output esperado*

  ```
  *
  **
  ***
  ****
  *****
  ```
]

#problem()[
  Crie um programa em C++ que recebe um valor numérico digitado pelo usuário,
  incremente esse número em uma unidade e exiba para o usuário.

  *Exemplo de input*
  ```
  Digite o valor de N: 5
  ```

  *Output esperado*
  ```
  N+1 = 6
  ```
]

#problem()[
  Crie um programa em C++ faz a leitura de dois valores numéricos digitados pelo
  usuário e troque o valor entre eles.

  *Exemplo de input*
  ```
  Digite o valor de X: 2
  Digite o valor de Y: 7
  ```

  *Output esperado*
  ```
  X = 7
  Y = 2
  ```
]
