#import "../../../../themes/inteli/lib.typ": *

#problem()[
  Crie uma classe `Retangulo` em C++ com atributos privados `largura` e `altura`. Implemente métodos públicos para definir os valores, calcular a área e calcular o perímetro. No programa principal, crie um objeto, defina as dimensões e imprima a área e o perímetro.
]
