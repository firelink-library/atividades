#import "../../../../themes/inteli/lib.typ": *

#problem()[
  Crie uma classe `ContaBancaria` em C++ com atributos privados `titular` (string), `saldo` (double) e `numeroConta` (int). Implemente métodos para: depositar um valor, sacar um valor (apenas se houver saldo suficiente), consultar o saldo atual e exibir os dados da conta. No programa principal, crie uma conta, realize operações de depósito e saque, e exiba o saldo final.
]
